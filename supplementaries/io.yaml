bioconductor:
  read_bed: |
    result = import(file)
  read_bam: |
    result = import(file)
  read_gtf: |
    result = import(file)

pyranges:
  read_bed: |
    result = pr.read_bed(f)
  read_bam: |
    result = pr.read_bam(f)
  read_gtf: |
    result = pr.read_gtf(f, annotation="ensembl")
